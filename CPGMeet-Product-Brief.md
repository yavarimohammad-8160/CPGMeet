# CPGMeet — سند کوتاه محصول (جلسات / کلندر)
**نسخه:** 0.2 · **تاریخ:** ۱۴۰۴/۰۶/۲۹ (۲۰۲۶-۰۹-۲۰) · **وضعیت:** MVP فاز ۱ در حال پیاده‌سازی / آماده اجرا

## ۱. تصمیم معماری
اپ **جدا** به نام کاری **CPGMeet** (مدیریت جلسات + کلندر).  
CPGChat دست‌نخورده می‌ماند (پروداکشن). اتصال نوتیف داخل چت **فاز۲** است؛ هویت از روز اول با JWT مشترک و `user_id` منطبق با CPGChat است.

| | فاز ۱ (MVP) | فاز ۲ |
|--|-------------|--------|
| محصول | CPGMeet مستقل | نوتیف + deep-link داخل CPGChat |
| کاربران | لاگین/دایرکتوری از API CPGChat | همان |
| نوتیف | دسکتاپ داخل CPGMeet (+ Socket.IO) | + اعلان داخل CPGChat (`CPGCHAT_NOTIFY_URL`) |
| ریسک چت | صفر | کم (فقط API نوتیف) |

## ۲. مشکل
ایمیل شرکت روی cPanel است؛ کلندر/جلسه واقعی نیست؛ IT همکاری نمی‌کند. همکاران با پیام پراکنده هماهنگ می‌شوند و جلسه را فراموش می‌کنند.

## ۳. هدف
نرم‌افزار سبک جلسات که:
- ساخت و دیدن جلسه راحت باشد
- کلندر داخلی داشته باشد
- **۱۵ دقیقه قبل** یادآوری بدهد
- نوتیف روی **سیستم کاربر** دیده شود
- بعداً همان یادآوری/دعوت داخل **CPGChat** هم برسد

## ۴. کاربران
همان افراد سازمان (همان حساب CPGChat). نقش‌ها: برگزارکننده، مدعو، بعداً ادمین.

## ۵. MVP فاز ۱ (CPGMeet)
1. لاگین با حساب CPGChat (JWT مشترک) — یک رمز، دو اپ  
2. ایجاد/ویرایش/لغو جلسه: عنوان، زمان، مکان/لینک، توضیحات، شرکت‌کنندگان  
3. RSVP: قبول / رد / شاید  
4. کلندر هفتگی + لیست «جلسات من»  
5. خروجی `.ics`  
6. جاب یادآوری ۱۵ دقیقه قبل → Desktop Notification در CPGMeet  
7. UI فارسی RTL ساده

### خارج از فاز ۱
همگام Google/Outlook، تکرار جلسات، رزرو اتاق، SMTP اجباری، گروه چت خودکار.

## ۶. فاز ۲ — اتصال به CPGChat
- env stub از قبل: `CPGCHAT_NOTIFY_URL`
- endpoint امن در سرور چت: مثلاً `POST /api/internal/notify` با secret سرویس  
- رویدادها: دعوت، تغییر/لغو، یادآوری ۱۵ دقیقه  

## ۷. فنی
- مسیر: `D:\Projects\cpgmeet`
- استک: React+Vite + Express + sql.js + Socket.IO
- پورت: web `5174`, api `8788`
- جداول: `meetings`, `meeting_participants` (+ `reminded_at`)

## ۸. معیار موفقیت
- جلسه زیر ۱ دقیقه ساخته شود  
- RSVP بدون آموزش  
- روی ۲ کلاینت، ۱۵ دقیقه قبل نوتیف دسکتاپ CPGMeet برسد  
- CPGChat در فاز ۱ بدون تغییر اجباری پایدار بماند  

## ۹. جمع‌بندی یک خطی
**CPGMeet اپ جدا، آماده اتصال؛ فاز ۱ کلندر+RSVP+یادآوری دسکتاپ؛ فاز ۲ نوتیف داخل CPGChat.**
